extends ../layout

block content
  .members
    - var parsedMembers = JSON.parse(members);
    each member in parsedMembers
      .col-sm-4
        .member
          .row
            p.member-name #{member.name}
            img(src="#{member.propic}", class='profile', width='100', height='100')
            p.member-tagline #{member.tagline}
          .row.member-btn
            a.btn.btn-default.btn-sm.page-scroll(href='/explore/members/' + member.memberID)
              i.fa.fa-paper-plane
              | Stalk this Member!